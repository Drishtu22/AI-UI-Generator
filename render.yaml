services:
# =========================
  # 🎨 Frontend Service
  # =========================
  - type: web
    name: ai-ui-frontend
    env: static
    rootDir: frontend
    buildCommand: npm install && npm run build
    staticPublishPath: build
    autoDeploy: true
    envVars:
      - key: REACT_APP_API_URL
        value: https://ai-ui-backend.onrender.com/api
